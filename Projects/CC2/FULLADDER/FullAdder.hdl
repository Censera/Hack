CHIP FullAdder {
    IN a, b, c;
    OUT sum,
        carry;

    PARTS:
        HalfAdder(a=a, b=b, sum=absum, carry=abcry);
        HalfAdder(a=absum, b=c , sum=sum, carry=tmp);
        Or(a=tmp, b=abcry, out=carry);
}
